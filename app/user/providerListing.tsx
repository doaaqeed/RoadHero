import React from "react";
import { SafeAreaView, StyleSheet, Text, View } from "react-native";

export default function RequestPending() {
  return (
    <SafeAreaView>
      <View>
        <Text>provider list</Text>
      </View>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({});
